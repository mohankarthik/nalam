[flake8]
max-line-length = 100
extend-ignore =
    # black puts a space before the colon in slices; PEP 8 agrees and E203 does not.
    E203,
    # line-break before a binary operator -- black's style, and the readable one.
    W503
exclude = venv,.git,__pycache__,build,dist
